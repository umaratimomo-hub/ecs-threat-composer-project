name: 2. Infrastructure Lifecycle (Terraform)

on:
  pull_request:
    branches: [ main ]
    paths:
      - 'infra/**'
  push:
    branches: [ main ]
    paths:
      - 'infra/**'
  workflow_dispatch:
    inputs:
      action:
        description: 'Action to perform'
        required: true
        type: choice
        options:
          - plan
          - apply
          - destroy
        default: 'plan'
      confirm_destroy:
        description: 'Safety Catch (ONLY if destroying): Type "DESTROY" to confirm'
        required: false
        type: string
        default: ''

permissions:
  id-token: write
  contents: read
  pull-requests: write

env:
  AWS_REGION: ${{ vars.AWS_REGION }}
  AWS_ROLE_ARN: ${{ vars.AWS_OIDC_ROLE_ARN }}
  TF_STATE_BUCKET: ${{ vars.TF_STATE_BUCKET }}
  PROJECT_NAME: ${{ vars.PROJECT_NAME }}
  TF_VAR_cloudflare_api_token: ${{ secrets.CLOUDFLARE_API_TOKEN }}
  TF_VAR_cloudflare_zone_id: ${{ secrets.CLOUDFLARE_ZONE_ID }}

jobs:
  terraform:
    name: Infrastructure Automation
    runs-on: ubuntu-latest

    defaults:
      run:
        working-directory: infra

    steps:
      - name: Checkout Code
        uses: actions/checkout@v4

      - name: Verify Teardown Confirmation
        if: github.event_name == 'workflow_dispatch' && inputs.action == 'destroy' && inputs.confirm_destroy != 'DESTROY'
        run: |
          echo "ERROR: Confirmation failed. You must type DESTROY to tear down the infrastructure."
          exit 1

      - name: Setup TFLint
        if: inputs.action != 'destroy'
        uses: terraform-linters/setup-tflint@v4
        with:
          tflint_version: latest

      - name: Run TFLint
        if: inputs.action != 'destroy'
        run: |
          tflint --init
          tflint -f compact --minimum-failure-severity=error

      - name: Run Trivy Vulnerability Scanner
        if: inputs.action != 'destroy'
        uses: aquasecurity/trivy-action@0.35.0
        with:
          scan-type: 'config'
          scan-ref: 'infra'
          format: 'table'
          exit-code: '1'
          severity: 'CRITICAL,HIGH'

      - name: Setup Terraform
        uses: hashicorp/setup-terraform@v3

      - name: Check Formatting
        if: inputs.action != 'destroy'
        run: terraform fmt -check -recursive

      - name: Configure AWS Credentials via OIDC
        uses: aws-actions/configure-aws-credentials@v4
        with:
          role-to-assume: ${{ env.AWS_ROLE_ARN }}
          aws-region: ${{ env.AWS_REGION }}

      - name: Terraform Init
        env:
          BUCKET: ${{ env.TF_STATE_BUCKET }}
          REGION: ${{ env.AWS_REGION }}
        run: |
          terraform init -input=false \
            -backend-config="bucket=$BUCKET" \
            -backend-config="region=$REGION"

      - name: Validate Configuration
        if: inputs.action != 'destroy'
        run: terraform validate

      - name: Terraform Plan
        id: plan
        if: github.event_name == 'pull_request' || (github.event_name == 'workflow_dispatch' && (inputs.action == 'plan' || inputs.action == 'apply')) || (github.event_name == 'push' && inputs.action == null)
        env:
          PROJ: ${{ env.PROJECT_NAME }}
        run: terraform plan -no-color -input=false -var="project_name=$PROJ" -var="domain_name=anaqahwear.com"

      - name: Update Pull Request with Plan
        uses: actions/github-script@v7
        if: github.event_name == 'pull_request'
        env:
          PLAN: "terraform\n${{ steps.plan.outputs.stdout }}"
        with:
          github-token: ${{ secrets.GITHUB_TOKEN }}
          script: |
            const output = `#### Terraform Plan 📖 \`${process.env.PLAN ? 'Success' : 'Failed'}\`

            <details><summary>Show Plan</summary>

            \`\`\`
            ${process.env.PLAN}
            \`\`\`

            </details>

            *Pusher: @${context.actor}, Action: \`${context.eventName}\`*`;

            github.rest.issues.createComment({
              issue_number: context.issue.number,
              owner: context.repo.owner,
              repo: context.repo.repo,
              body: output
            })

      - name: Terraform Apply
        if: github.event_name == 'push' || (github.event_name == 'workflow_dispatch' && inputs.action == 'apply')
        env:
          PROJ: ${{ env.PROJECT_NAME }}
        run: terraform apply -auto-approve -input=false -var="project_name=$PROJ" -var="domain_name=anaqahwear.com"

      - name: Terraform Destroy
        if: github.event_name == 'workflow_dispatch' && inputs.action == 'destroy'
        env:
          PROJ: ${{ env.PROJECT_NAME }}
        run: terraform destroy -auto-approve -input=false -var="project_name=$PROJ" -var="domain_name=anaqahwear.com"